export function SeoBar() {
  return (
    <div className="w-full border-b border-[var(--border)] bg-white px-4 py-3">
      <div className="mx-auto max-w-6xl text-center text-sm leading-relaxed text-[var(--muted)]">
        <p>
          Trim, concat, and remux video online with FFmpeg — stream-copy when possible for fast,
          lossless cuts.
        </p>
        <p>
          Generate thumbnails, contact sheets, sprite sheets with WebVTT, and detect shot
          boundaries — no ads, no account required.
        </p>
      </div>
    </div>
  );
}
