import { Playground } from '@/components/Playground';

export const metadata = {
  title: 'Video Contact Sheet — ClipTools',
  description: 'Grid montage contact sheets with timestamps from your video.',
};

export default function ContactSheetPage() {
  return <Playground />;
}
