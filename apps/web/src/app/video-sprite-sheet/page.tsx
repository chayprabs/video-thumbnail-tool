import { Playground } from '@/components/Playground';

export const metadata = {
  title: 'Video Sprite Sheet + WebVTT — ClipTools',
  description: 'Create sprite sheets with WebVTT for hover-scrub video players.',
};

export default function SpritePage() {
  return <Playground defaultTab="sprite" />;
}
